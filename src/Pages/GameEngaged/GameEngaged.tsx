import React from 'react'

export default function GameEngaged() {
  // if game is local, connect to created player objects, no need to save data
  // if game is online. Need to have game key - in database. Store game key, and users figure out how to connect live- should be able to allow access to page with database and path/:id

  return (
    <div>GameEngaged</div>
  )
}
